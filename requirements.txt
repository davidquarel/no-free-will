fastapi>=0.110
uvicorn[standard]>=0.29
# Required only for real model inference (not needed for MODEL_NAME=mock):
torch>=2.1
transformers>=5.10   # Qwen3 + Gemma 4 (gemma4_unified) architecture support
accelerate>=0.26     # efficient/low-memory weight loading + GPU sharding
bitsandbytes>=0.43   # 4-bit/8-bit quantization (CUDA, Linux) for QUANTIZE=4bit
gpustat>=1.1         # live GPU monitor panel (/gpu) — nvml-based
